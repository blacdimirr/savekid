plugins {
    // Provides Foojay-based toolchain repository resolution for Gradle toolchain downloads
    id "org.gradle.toolchains.foojay-resolver-convention" version "0.8.0"
}

rootProject.name = "tracker-server"
